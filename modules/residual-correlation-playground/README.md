# Residual Correlation Playground

A standalone, no-build JavaScript module that illustrates candidate-unit selection in Cascade-Correlation.

The module uses a toy XOR residual pattern. Sliders control a candidate tanh unit. The visualization shows candidate activations, residuals, Pearson correlation, and a small network sketch.
