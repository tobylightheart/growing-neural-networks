# Tasks

> Read this file at the start of every session, then consult
> `.tasks/LOG.jsonl`. Follow the generic `work-cycle` skill.

## Project

**Name:** Growing Neural Networks literature garden
**Repository:** `/workspace/growing-neural-networks`
**Task prefix:** `GNN`

## Current focus

No task is currently queued. GNN-3 completed the bounded private-source
verification of the core Growing When Required mechanism seam. Its worksheet
remains an automated, not-human-reviewed aid; promoting its findings into the
public Marsland review requires a later human review decision.

## Queue

`.tasks/LOG.jsonl` is authoritative. An empty queue is a valid no-op for cron.

## Task files

- Open work orders: `.tasks/<task-id>-<slug>.md`
- Completion debriefs: `.tasks/debriefs/<task-id>-<slug>.md`
