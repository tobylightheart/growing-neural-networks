# Interactive modules

Each substantial visualization or playground should live in its own directory under `modules/<module-id>/` with a `module.json`, `index.html`, local JavaScript, and local CSS if needed. Shared primitives belong in `assets/js/`.
